# CLAUDE.md — Claude Code entry point

@skills/comsol-64-operations/SKILL.md

---

The import above pulls in the full `comsol-64-operations` skill (COMSOL 6.4+ with MPh 1.3.1 standalone / clientapi operations guide). Claude Code will use it automatically when a task matches the skill's `description` frontmatter.

For tool-agnostic project rules also readable by Codex / Gemini CLI / Cursor, see `AGENTS.md`.
